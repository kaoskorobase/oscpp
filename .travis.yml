addons:
  apt:
    sources:
      - llvm-toolchain-precise
      - ubuntu-toolchain-r-test
    packages:
      - clang-3.7
      - g++-5
      - gcc-5
language: cpp
script: make
env:
  - MY_CXX=g++-5
  - MY_CXX=clang++-3.7
install:
  - export CXX="${MY_CXX}"
